import React from 'react';
declare type Props = {
    appendMessage?: string;
    onClick?: React.MouseEventHandler<HTMLButtonElement>;
};
export declare const Test: ({ appendMessage, onClick }: Props) => JSX.Element;
export {};
